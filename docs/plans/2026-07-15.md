# Daily Plan — 2026-07-15

Auto-generated by product-manager (scheduled 7:03 AM weekday run). No stakeholder
present for this run — priorities set by judgment call per the pm-daily-plan
routine's triage rules. See notes at the bottom for calls that need operator
confirmation.

## Source: today's to-do list (from `docs/product/epic-status.md`)

Items currently ☐ planned / 🔄 in flight / ⏳ partially done:

1. **E2 · Build 2** (⏳ partially done, 75%) — outstanding items:
   - Run migration `0002_build2_activity_log.sql` against production Supabase
   - Run migration `0003_build2_orders.sql` against production Supabase
   - QA pass on commits `acf8f50` / `6112228` / `fd98971` (Orders, People directory, Newsletter, admin nav, Resend notification) — no QA-REPORT exists yet
   - Update `CLAUDE.md` Build 2 status line — currently stale, predates last night's `acf8f50` merge
   - (By-design, not today's scope) Visitor confirmation email — blocked on `jasper-ai.com` domain purchase

E1 · Build 1 is ✅ done — nothing to plan today.

## Today's 3 priorities

### 1. QA pass on Build 2 (Orders / People directory / Newsletter / Resend notification / admin nav restructure)
- **Rationale:** Highest pipeline stage of any outstanding item — code is feature-complete (merged in `acf8f50`, `6112228`, `fd98971`) but untested. This is the single biggest gap between "looks done" and "is done" for Build 2.
- **Risk:** Medium — touches the live database read paths and a live Resend send; not pure config.
- **Assigned:** qa (via developer handoff, per the existing pattern in `docs/engineering/changes/`)
- **Status:** Pending review (not auto-approved — touches external services)
- **Acceptance criteria (user story format):**
  - As the operator, I want the Orders page verified end-to-end, so that recording a purchase against a person is reliable before I rely on it.
    - Given a person exists in `people`, when I record an order via the admin Orders form, then a row appears in `orders` and on that person's history drawer.
  - As the operator, I want the People directory search verified, so that I can find any contact without opening Supabase.
    - Given at least one seeded person, when I search by name, email, or company, then the matching row(s) appear and non-matching rows do not.
  - As the operator, I want the Resend notification path verified again after two hotfixes, so that I know internal notifications are reliably landing.
    - Given a contact-form submission, when the form is submitted, then a notification email is received at the current registered address and the submit itself never blocks on email failure.

### 2. Run pending Supabase migrations (`0002_build2_activity_log.sql`, `0003_build2_orders.sql`) against production
- **Rationale:** Explicitly flagged in the QA-REPORT and in commit `acf8f50` as a manual step outside agent scope — until this runs, Orders doesn't work live and status changes carry no audit trail. This is the highest-leverage unblock available today.
- **Risk:** Low-medium (schema-only migration, already reviewed in the commit), but requires direct Supabase SQL Editor access — outside the developer agent's normal automated path.
- **Assigned:** Operator (Jasper) — flagged as a blocker, not auto-approved to an agent
- **Status:** Blocked — awaiting operator action
- **Acceptance criteria:**
  - As the operator, I want both pending migrations applied to production, so that Orders and the activity log work in the live app.
    - Given the Supabase SQL Editor, when I run `0002_build2_activity_log.sql` then `0003_build2_orders.sql`, then subsequent lead moves write an `activity_log` row and the Orders page reads/writes without error.

### 3. Refresh stale docs — `CLAUDE.md` Build 2 status line + file a QA-REPORT stub for the Build 2 scope
- **Rationale:** `CLAUDE.md` still says Build 2 is missing Orders/Newsletter/login, which is now false as of last night's merge — any agent reading it first (per its own "read before any /goal command" instruction) would work from stale state. Low-risk, content-only fix.
- **Risk:** Low — documentation/config only, no code change, no external calls.
- **Assigned:** developer (doc update only)
- **Status:** ✅ Auto-approved at 07:03 (high priority + low risk: content-only, no new code, no external API calls)
- **Acceptance criteria:**
  - As any agent reading `CLAUDE.md`, I want the Build 2 status line to reflect the current git state, so that I don't re-derive or contradict already-shipped work.
    - Given commits `acf8f50`/`6112228`/`fd98971` are merged, when `CLAUDE.md` is updated, then the Build 2 line lists Orders/People/Newsletter/nav/Resend-notification as built and names the two pending migrations as the remaining gap.

## Blockers

- **Two Supabase migrations not applied to production** (`0002`, `0003`) — blocks Orders and activity-log audit trail from working live. Requires operator SQL Editor access; not resolvable by an agent today.
- **`jasper-ai.com` domain not yet purchased** — blocks Resend sending-domain verification and the visitor-facing confirmation email (deliberately deferred, per commit `acf8f50`, not a defect). Operator action, external dependency.
- **No stakeholder approval available this run** (scheduled autonomous execution) — priority #1 (QA pass) and priority #2 (migrations) are NOT auto-approved per the triage rule (medium risk / external dependency / requires operator access) and remain pending review.

## Auto-approval log

- Auto-approved "Refresh stale `CLAUDE.md` Build 2 status line" at 07:03 — high priority, low risk (content-only, no new code, no external API calls).
- Everything else above is pending review — logged as Blocked or Pending in `docs/project-status.html`.

## Open decision flagged for the operator

`docs/product/epics.md` and `docs/product/product.md` do not exist for this project — only `product-plan.md` (BUILD 1/BUILD 2 spec). Today's epic-status.md was reconstructed from git history + CLAUDE.md rather than a formal epics doc. Recommend a short product-strategy session to properly scaffold `docs/product/product.md` + `docs/product/epics.md` per the standard format — no deadline forced, flagging for whenever the operator has 20 minutes.
