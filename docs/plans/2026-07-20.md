# Daily Plan — 2026-07-20

**Run:** pm-daily-plan, 07:03 (scheduled, autonomous — no stakeholder present)
**Source of truth for to-do extraction:** `docs/product/epic-status.md` (as of its last content update, 2026-07-20 corrections; header timestamp on that file is stale at "2026-07-16 19:15" — flagged below, not corrected by this run)

---

## Step 1 — To-do list extracted from epic-status.md

Items with status ☐ planned / 🔄 in flight / ⏳ partially done:

| Epic | Item | Status | Pipeline stage | Open bugs |
|---|---|---|---|---|
| E2 · Build 2 | No QA-REPORT filed for Orders/People/Newsletter/Resend-notification/nav-restructure scope; Apple-minimalist redesign also has no QA pass | ⏳ 75% | 3 · Feature-complete (not yet 4 · Tested) | 0 |
| E4 · Build 3 | Register `dashboard-baseline-weekly` (E4.1) and `content-brief-weekly` (E4.3) as local scheduled tasks; observe one live cycle of each once registered | 🔄 90% | 3 · Feature-complete (code-complete, not yet operational) | 0 |

E1 and E3 are not on today's to-do list (E1 ✅ 100% done; E3 marked ✅ verified 90%, but see correction below).

**Correction found during this run (not in epic-status.md yet):** E3's "Outstanding" section claims the `buildNeedsAttention()` fix is "still uncommitted in the working tree — not shippable until committed and pushed via PR." This is stale. `git log -S "slice(0, 8)" -- website/src/lib/dashboard.ts` shows the fix landed in commit `087147a`, merged to `main` via PR #11 (`68ad5e7`), and is present in the current `main` build of `website/src/lib/dashboard.ts` (no `.slice(0, 8)` remains). E3 is functionally further along than its stated 90% suggests. Flagging for the developer/PM to correct `docs/product/epic-status.md` next update — not editing it directly in this run since PM's mandate here is planning, and epic-status.md is the developer agent's file to correct per its own "How this file gets updated" rule.

## Step 2 — Git activity & check-ins (last 48h)

`git log --oneline --since="48 hours ago"` shows 8 commits, all already reflected in epic-status.md: PR #17 (5 bottom-funnel briefs), PR #18 (linkedin-repurpose-weekly routine, unregistered), PR #19 (Vercel Analytics), PR #20 (epic-status.md registration-claim correction), plus the underlying commits.

No `standup/individual/` directory exists in this repo (solo operator — no team check-ins to compile). No new blockers surfaced here beyond what's already tracked.

`content/content-calendar/2026-07-first-post.md` — only one entry, already published (per E4.3). No new queue items to flag.

Confirmed against `~/.claude/scheduled-tasks/`: registered routines are `designer-weekly`, `developer-daily`, `devops-daily`, `email-marketer-weekly`, `pm-daily-plan`, `pm-eod-summary`, `pm-standup-compile`, `pm-weekly-contribution`, `pm-weekly-rag`, `web-publisher-weekly`, `writer-weekly`. This independently confirms epic-status.md's 2026-07-20 correction: `dashboard-baseline-weekly`, `content-brief-weekly`, and `linkedin-repurpose-weekly` are genuinely absent — none of the three exist on disk.

Also found (not yet in epic-status.md's to-do list, surfaced in `docs/plans/2026-07-20-content-leadgen-optimization.md`, an earlier same-day strategy review): the content-attribution widget on `/admin` still renders seed data tagged `attributes.source = "seed:content-attribution"` — should be cleaned up before those numbers are treated as real signal. Adding as priority 3 candidate below since it's a live-site data-integrity issue, not just an automation gap.

## Step 3 — Today's 3 priorities

Ranked by unblocked + highest pipeline stage first, then value:

### 1. File the missing QA-REPORT for E2 Build 2 CRM scope
- **Assigned:** qa
- **Risk:** Medium — reads live production Supabase data and exercises production auth gating (no code changes, no writes expected, but touches production surfaces — consistent with how the same class of task was risk-rated on 2026-07-16)
- **Why first:** E2 has been stuck at 75%/pipeline-stage-3 since 2026-07-15; this is the single item blocking it from advancing to "Tested." Everything it covers (Orders, People, Newsletter, Resend, nav, redesign) has been live for 5 days with zero formal verification.
- **Definition of done:** A `QA-REPORT.md` filed under `docs/engineering/changes/2026-07/` covering: Orders (create/lookup-by-email), People directory (search/filter/drawer), Newsletter (ok_to_contact filter), Resend internal notification (non-blocking, correct recipient), nav/logout on the `(dashboard)` route group, and the Apple-minimalist redesign (visual regression / no broken layout) — each item marked PASS/FAIL with evidence, consistent with the format of the four existing QA-REPORTs in that directory.
- **Status:** ⏳ Awaiting Approval — not auto-approved (medium risk, touches production data/auth).

### 2. Clean up seed-tagged demo data in the content-attribution widget
- **Assigned:** developer
- **Risk:** Medium — likely requires deleting or filtering rows in production Supabase (`attributes.source = "seed:content-attribution"`); per engineering rules, deleting data needs explicit confirmation before execution, so this cannot be nodded through sight-unseen.
- **Why second:** Directly affects trust in the dashboard's content-attribution numbers, which is the whole point of E4.3's channel-signal automation — feeding it seed data risks a wrong channel bias once real data volume grows.
- **Definition of done:** Either (a) the seed-tagged rows are removed from production Supabase (with an explicit before/after row-count check, per the precedent set in the 2026-07-19 QA cleanup pattern), or (b) the dashboard query explicitly excludes rows where `attributes.source` starts with `seed:` — operator to confirm which approach before execution, since (a) is a destructive operation.
- **Status:** ⏳ Awaiting Approval — not auto-approved (touches production data, destructive-operation adjacent).

### 3. Desktop registration of three pending local routines (operator-only)
- **Owner:** Operator (Jasper) — cannot be done from this CLI/scheduled-run session
- **Items:** `dashboard-baseline-weekly` (E4.1, from the E4.1 script/routine file), `content-brief-weekly` (E4.3, from `website/scripts/E4.3-content-brief-routine.SKILL.md`), `linkedin-repurpose-weekly` (from `website/scripts/E-linkedin-repurpose-routine.SKILL.md`) — plus changing `writer-weekly`'s cadence from weekly to biweekly.
- **Risk:** N/A — not an agent task, requires the operator in Claude Desktop using the `create-local-routine` skill.
- **Why flagged, not scheduled:** All engineering/content work behind these three routines is already code-complete and merged (per E4 in epic-status.md); the only remaining step is registration, which this environment cannot perform. Blocking E4 from advancing past 90%.
- **Status:** 🛑 Blocked — operator action required.

## Auto-approval triage (07:03)

No item today is both **high priority** and **low risk** (no new code / no external calls / content-or-config-only) as defined by the daily-plan gate:
- Priority 1 (QA-REPORT) — high priority, but medium risk (production data + auth surfaces) → **not auto-approved**.
- Priority 2 (seed data cleanup) — medium priority, medium/destructive-adjacent risk → **not auto-approved**.
- Priority 3 (Desktop registration) — not an agent task at all → **not applicable to auto-approval; logged as a blocker**.

Result: all items → **Backlogged — awaiting approval**. Nothing moves to the developer's 9 AM pickup queue automatically today. If the operator reviews and approves priority 1 and/or 2 before 9 AM, they can be manually flipped to ✅ Approved in `docs/project-status.html`.

## Step 4.5 — Team contributions (Mondays)

Today is Monday, but `scripts/team-hours.py` does not exist in this repo (checked `scripts/`), and there is no `docs/assessments/team-hours-methodology.md` to follow. This project is a solo-operator build (per `CLAUDE.md`) with no team-hours tracking set up. Skipping this step — flagging as an open decision (does the team-contributions section even apply to a solo-operator project?) rather than fabricating a script run.

## Step 5 — Notify

`LARK_WEBHOOK_URL` (and `LARK_APP_ID` / `LARK_APP_SECRET`) are unset in both `.env.local` and `.env.example` (present only as commented-out placeholders). Per the SKILL's own conditional ("If `LARK_WEBHOOK_URL` is set"), skipping the Lark notification. No stakeholder message was sent.

## Blockers

- `dashboard-baseline-weekly`, `content-brief-weekly`, `linkedin-repurpose-weekly` registration, and the `writer-weekly` cadence change — all require the operator in Claude Desktop; cannot be done from this session (carried over from the 2026-07-20 content-leadgen review).
- `docs/product/epic-status.md`'s header timestamp ("Last updated: 2026-07-16 19:15") is stale relative to its own body content (which has 2026-07-20 corrections) — cosmetic, but worth fixing next time the file is touched.
- `docs/product/epic-status.md`'s E3 "Outstanding" section incorrectly states the needs-attention fix is uncommitted; it shipped via PR #11 (commit `087147a`) and is live on `main`. Recommend the developer agent correct this on its next pass.
- `jasper-ai.com` still not purchased (carried over since 2026-07-14) — continues to block the visitor-facing confirmation email and full E2 completion criteria.
