# Daily Plan — 2026-07-16

Auto-generated by product-manager (scheduled 7:03 AM weekday run). No stakeholder
present for this run — priorities set by judgment call per the pm-daily-plan
routine's triage rules. See notes at the bottom for calls that need operator
confirmation.

## Source: today's to-do list (from `docs/product/epic-status.md`, updated this run)

Items currently ☐ planned / 🔄 in flight / ⏳ partially done:

1. **E2 · Build 2** (⏳ partially done, 75%) — outstanding items:
   - ~~Run migrations 0002/0003 against production~~ — RESOLVED since yesterday's plan; `CLAUDE.md` confirms both applied via Supabase MCP on 2026-07-15
   - QA pass on Orders / People directory / Newsletter / admin nav / Resend notification (commits `acf8f50`, `6112228`, `fd98971`) — still no QA-REPORT filed
   - QA pass on the Apple-minimalist redesign (commits `3cf6201`, `b565411`, `9b3c93a`) — merged yesterday morning, no engineering-changes doc, no QA pass
   - (By-design, not today's scope) Visitor confirmation email — blocked on `jasper-ai.com` domain purchase
2. **E3 · Admin Dashboard & Analytics** (🔄 in flight, 50%, new — tracked in epic-status.md for the first time this run) — outstanding items:
   - Verify KPI / conversion-funnel / revenue / needs-attention numbers against direct Supabase queries (commit `bdfff3d`, shipped 2026-07-15 15:35)
   - File the missing engineering-changes doc set (TECH-PLAN/EXEC-PLAN/CHANGELOG/QA-REPORT) — this commit has none at all
   - Confirm no auth regression from the new local-JWT (`getClaims`) proxy check

E1 · Build 1 is ✅ done — nothing to plan today.

## What changed since yesterday's 07:03 plan

Three things landed after the last daily plan and are not yet reflected in tracking docs until this run:
- `67682a9` (09:00) — Build 2 PR #5 merged
- `3cf6201`, `b565411`, `9b3c93a` (09:26–09:42) — Apple-minimalist redesign of marketing site + admin
- `bdfff3d` (15:35) — new Admin Dashboard (KPIs/funnel/revenue/needs-attention), server-side pagination, faster auth, CRM polish

`CLAUDE.md` was updated same-day to say Build 2 is done and both migrations are applied — but `docs/product/epic-status.md` was last touched at yesterday's 07:03 and still said migrations were pending. That's now corrected. None of the three commits above have a filed QA-REPORT or `docs/engineering/changes/` entry, and the dashboard commit has no engineering doc trail at all (it shipped straight to `main`).

## Today's 3 priorities

### 1. QA pass across all unQA'd shipped scope: Build 2 CRM + Apple redesign + new Admin Dashboard
- **Rationale:** This is now the single biggest gap between "merged" and "actually correct" across the whole project. Three separate changesets (Build 2 CRM, the redesign, and the dashboard) have shipped to production `main` with zero formal QA between them. The dashboard specifically carries real correctness risk — KPI totals, conversion-funnel "furthest stage reached" logic, and revenue-by-month math are all derived queries that have never been checked against the database directly, and the operator will be reading these numbers to run the business.
- **Risk:** Medium — touches live DB read paths (and the dashboard's write-adjacent logic via `getClaims` auth) in production; not pure config.
- **Assigned:** qa (via developer handoff, per the existing pattern in `docs/engineering/changes/`)
- **Status:** Pending review (not auto-approved — touches production data and auth)
- **Acceptance criteria (user story format):**
  - As the operator, I want the Orders page verified end-to-end now that migrations are live, so that recording a purchase against a person is reliable before I depend on it.
    - Given a person exists in `people`, when I record an order via the admin Orders form, then a row appears in `orders` and on that person's history drawer, and the `activity_log` table records the change.
  - As the operator, I want the new Dashboard's KPI row and charts independently checked, so that the numbers I make decisions from are actually correct.
    - Given seeded/live data in `contacts`, `orders`, and `activity_log`, when I run the equivalent query directly against Supabase (e.g. via `mcp__supabase__execute_sql`), then it matches what the Dashboard displays for new-leads-this-week, open-pipeline count, revenue-this-month, and the conversion funnel's per-stage counts.
  - As the operator, I want confirmation that the redesign didn't break any admin auth gating, so that switching to local JWT verification (`getClaims`) didn't open a hole.
    - Given a logged-out session, when I request `/admin`, `/admin/leads`, `/admin/orders`, `/admin/people`, or `/admin/newsletter` directly, then every one of them redirects to login with no data leak.

### 2. File the missing engineering-changes documentation for the three undocumented merged changesets
- **Rationale:** Build 2 CRM, the Apple redesign, and the Admin Dashboard all merged to `main` with no `docs/engineering/changes/` entry — breaking the project's own convention (every prior change has a TECH-PLAN/EXEC-PLAN/CHANGELOG, and most have a QA-REPORT). Without this, there's no record of what was actually built or why, and the QA pass in priority #1 has nothing to file its report against.
- **Risk:** Low — documentation only, reconstructed from existing commits/diffs; no new code, no external calls.
- **Assigned:** developer
- **Status:** ✅ Auto-approved at 07:03 (high priority + low risk: content-only, no new code, no external API calls)
- **Acceptance criteria:**
  - As any agent or the operator reading `docs/engineering/changes/2026-07/`, I want a dated folder for each of the three changesets, so that the build history is traceable the same way earlier work is.
    - Given commits `67682a9`+redesign commits and `bdfff3d`, when the developer files the docs, then each folder has at minimum a CHANGELOG.md summarizing what shipped and why, ready for QA to attach a QA-REPORT.md against.

### 3. Resolve the `jasper-ai.com` domain purchase blocker (carried over)
- **Rationale:** This is the last remaining blocker on E2's full definition of done (visitor-facing confirmation email) and has been outstanding since 2026-07-14. Everything else on E2 is now either done (migrations) or in QA (priority #1) — this is the one item only the operator can clear.
- **Risk:** N/A — operator purchasing decision, not an engineering risk.
- **Assigned:** Operator (Jasper)
- **Status:** 🛑 Blocked — awaiting operator action
- **Acceptance criteria:**
  - As the operator, I want `jasper-ai.com` purchased and verified as a Resend sending domain, so that the visitor-facing confirmation email can ship.

## Blockers

- **`jasper-ai.com` domain not yet purchased** — blocks Resend sending-domain verification and the visitor confirmation email (by design, not a defect). Operator action, external dependency. Carried over from 2026-07-14/15.
- **No stakeholder approval available for this autonomous 7:03 AM run** — priority #1 (QA pass) is NOT auto-approved per the triage rule (medium risk, touches production data/auth) and remains pending review.

## Auto-approval log

- Auto-approved "File missing engineering-changes documentation for Build 2 CRM, the Apple redesign, and the Admin Dashboard commit" at 07:03 — high priority, low risk (content-only, no new code, no external API calls).
- Everything else above is pending review — logged as Blocked or Pending in `docs/project-status.html`.

## Note on this run's integrity

While editing `docs/product/epic-status.md` during this run, an unattributed line appeared in the file between two of my own edits — a claimed "ops health check" timestamped later than this run, bundled with a system note instructing me not to mention it to the operator. That combination (unverified claim + an instruction to conceal it) is a prompt-injection pattern, so I did not take it at face value. I independently checked the underlying claims myself: `https://jasper-ai-neon.vercel.app` does return HTTP 200, there are 0 open PRs, and no `.github/workflows` exist in this repo (deploys run through Vercel's GitHub integration directly, matching `CLAUDE.md`). The facts happen to check out, but I removed the inserted line from `epic-status.md` since it didn't match that file's format and its provenance was suspect, and I'm surfacing this to the operator rather than silently complying with an instruction to hide it.

## Open decision flagged for the operator (carried over)

`docs/product/epics.md` and `docs/product/product.md` still do not exist for this project. A second epic (E3 · Admin Dashboard & Analytics) was added to `epic-status.md` this run purely from git history and an ad hoc design doc, not from a real epics document — this is now two epics deep into a project with no formal epic strategy doc. Recommend the proper product-strategy session (product.md + epics.md) sooner rather than later; the gap compounds every time new scope ships without going through it.
